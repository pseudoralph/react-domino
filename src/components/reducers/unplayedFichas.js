import initialState from './initialState';
import types from '../constants';

const unplayedFichas = (state = initialState().unplayedFichas, action) => {
  return state;
};

export default unplayedFichas;
